const pageStyle = {
  display: "grid",
  minHeight: "100svh",
  placeItems: "center",
  padding: 24,
} as const;

export default function MentorGroupsPage() {
  return (
    <main style={pageStyle}>
      <h1>Mentor groups module</h1>
    </main>
  );
}
