const pageStyle = {
  display: "grid",
  minHeight: "100svh",
  placeItems: "center",
  padding: 24,
} as const;

export default function MentorAvailabilityPage() {
  return (
    <main style={pageStyle}>
      <h1>Mentor availability module</h1>
    </main>
  );
}
